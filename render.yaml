services:
  - type: web
    name: student-gpa-dropout-predictor
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn student_gpa_dropout_app:app --bind 0.0.0.0:$PORT
